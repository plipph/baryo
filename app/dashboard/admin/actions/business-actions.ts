"use server"; import { revalidatePath } from "next/cache"; import { createClient } from "@/lib/supabase/server"; export async function toggleBusinessStatus( businessId: string, currentStatus: boolean ) { const supabase = await createClient(); const { data: { user }, } = await supabase.auth.getUser(); if (!user) { throw new Error( "Unauthorized" ); } /* TEMP ADMIN CHECK */ const adminEmails = [ "kgcomia@gmail.com", ]; const isAdmin = user.email && adminEmails.includes( user.email ); if (!isAdmin) { throw new Error( "Forbidden" ); } /* UPDATE BUSINESS STATUS */ const { error } = await supabase .from("businesses") .update({ is_active: !currentStatus, }) .eq("id", businessId); if (error) { throw new Error( error.message ); } /* REFRESH ADMIN PAGE */ revalidatePath( "/dashboard/admin" ); }